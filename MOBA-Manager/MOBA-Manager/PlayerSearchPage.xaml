<Page x:Class="MOBA_Manager.PlayerSearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MOBA_Manager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SquadPage">

    <Grid>
        <ListView x:Name="SquadListView" Margin="0" FontSize="14" FontWeight="Bold" Background="#FFE0E0E0" MouseDoubleClick="SquadListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContextMenu>
                        <ContextMenu HorizontalContentAlignment="Stretch" HorizontalOffset="20" MinWidth="200" Width="0"/>
                    </GridView.ColumnHeaderContextMenu>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Path=Name}" />
                    <GridViewColumn Header="Age" DisplayMemberBinding="{Binding Path=Age}"/>
                    <GridViewColumn Header="Role" DisplayMemberBinding="{Binding Path=Role}"/>
                    <GridViewColumn Header="Salary" DisplayMemberBinding="{Binding Path=Salary}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
